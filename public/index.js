// Requests: one for when you add an item to the database and one for displaying the data.
// The first should be triggered when you click the "submit" button.
// The second should be triggered on loading the page.
// var redis  = require("redis");
// var client = redis.createClient();

// var storeData = function (){
//   var xhr = new XMLHttpRequest();
//
//   xhr.onreadystatechange = function () {
//     // if (xhr.readyState == 4 && xhr.status == 200) {
//     //
//     // }
//     xhr.open("POST", "/", true);
//     xhr.send();
//   };
// };
